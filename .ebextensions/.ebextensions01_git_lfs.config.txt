commands:
  01_install_git_lfs:
    command: "curl -s https://packagecloud.io/install/repositories/github/git-lfs/script.rpm.sh | bash && yum install -y git-lfs && git lfs install"
  02_pull_lfs:
    command: "cd /var/app/staging && git lfs pull"